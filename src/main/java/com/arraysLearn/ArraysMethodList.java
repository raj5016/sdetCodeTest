package com.arraysLearn;

public class ArraysMethodList {
	
	/*
	 	1.Arrays.asList(arr1);
	 	2.Arrays.toString(arr1)
	 	3.Arrays.sort()
	 	4.Arrays.equals(arr1,arr2)
	 	5.Arrays.hascode(arr1)
	 	6.Arrays-fill
	 		a.Arrays.fill(arr1,value)
	 		b.Arrays.fill(Arr1,FromIndex,toIndex,value)
	 	7.Arrays-copyOf
	 		a.Arrays.copyOf(arr1)
	 		b.Arrays.copyOf(arr1,fromIndex,toIndex)
	 	8.Arrays.deepString(arr1)
	 	9.Arrays.deepEquals(arr1,arr2)--Two dimension arrays
	 	10.Arrays.deepHashCode(arr1,arr2)-Two dimension arrays
	 	11.Arrays-binarySearch
	 		a.Arrays.binarySearch(arr1,element)
	 		b.Arrays.binarySearch(arr1,fromIndex,toIndex,element)
	 	--------------------------------------------------------------------------------
	 	1.Integer
	 			Integer.compare(a,b)
	 			Integer.compareUnsigned(a,b)
	 	
	 	
	 */

}
